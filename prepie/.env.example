# prepie runs on seeded mock data with NO env vars set.
# Fill these in only when you're ready to wire the real database.

# ── Supabase (Prepi project: beznhteumkduzxpqyfci, us-east-1) ─────────────
# Copy the FULL transaction pooler URI from the dashboard — do not hand-build.
#
#   Supabase → Prepi → Connect → ORM / Drizzle → Transaction pooler (6543)
#
# Expected shape (password from Connect → Reveal):
#   postgresql://postgres.beznhteumkduzxpqyfci:***@aws-0-us-east-1.pooler.supabase.com:6543/postgres
#
# Wrong host (does not resolve): aws-us-east-1.pooler.supabase.com
#
# One command wires local + Vercel + redeploy:
#   npm run setup:db -- '<paste-full-uri-here>'
#
# Verify only:
#   npm run verify:db
DATABASE_URL=

# Later, for the P1 intelligence layer:
# ANTHROPIC_API_KEY=
